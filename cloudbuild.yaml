steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - -f
      - infra/Dockerfile.cpu
      - -t
      - $_IMAGE
      - .
    timeout: "1800s"
images:
  - $_IMAGE
timeout: "1800s"
options:
  machineType: "E2_HIGHCPU_8"
